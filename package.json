{
  "name": "spooltags-website",
  "version": "1.0.0",
  "description": "SpoolTags marketing website",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "dev:css": "tailwindcss -i ./src/input.css -o ./src/output.css --watch",
    "build": "npm run build:css && cp src/index.html dist/ && cp -r src/privacy dist/ && cp src/CNAME dist/ 2>/dev/null || true && cp -r src/images dist/ 2>/dev/null || true",
    "dev": "echo 'Run in two terminals: npm run dev:css AND npx live-server src'"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
